apiVersion: v1
kind: Service
metadata:
  name: {{ include "sms-checker-helm-chart.fullname" . }}
  labels:
    {{- include "sms-checker-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sms-checker-helm-chart.selectorLabels" . | nindent 4 }}
